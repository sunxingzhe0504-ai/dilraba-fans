"use client";

import { DailyQuote } from "@/components/DailyQuote";

/** Shared fan-facing strip on home — daily quote only (full calendar on /fans). */
export function HomeFanBand() {
  return <DailyQuote />;
}
